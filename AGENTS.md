# General Workspace Rules

- **Scratch Files**: Never commit scratch files (`scratch_*`, temporary scripts, etc.) into the repository. 
- **Cleanup**: Always remove scratch files as soon as you are done using them. 
- **Alternative**: Prefer using the `/tmp` directory or the designated artifact scratch directory for temporary files instead of the project workspace.
